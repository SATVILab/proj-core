1. **Create `src/profile.rs`**:
   - Implement `get_active_profiles()` to parse the `PROJR_PROFILE` environment variable (split by `,` or `;`, strip whitespace, reject "default" and "local").
   - Implement `create_profile(name)` to create `_projr-<name>.yml`.
   - Implement `create_local_profile()` to copy `_projr.yml` into `_projr-local.yml` and replace values with null (`~`).
   - Implement `delete_profile(name)` to delete `_projr-<name>.yml`.
   - Add these as commands to `main.rs` if necessary or just expose them as API.

2. **Update `src/yml.rs` for Cascading Profiles**:
   - Implement deep merge logic `deep_merge(root: serde_json::Value, profile: serde_json::Value, local: serde_json::Value) -> serde_json::Value`. Use `serde_json::Value` for merging to easily handle nulls backing off to lower tiers, and generic object merging.
   - Replace `ProjConfig` deserialization to use `serde_json::Value` first, do the merge, and then deserialize the merged value into `ProjConfig`.
   - Implement a top-level filtering function `yml_get_filter_top_level(value: serde_json::Value) -> serde_json::Value` which retains only `directories`, `build`, `dev`, `remotes` (wait, the prompt says "directories, build, dev, metadata", I need to verify if "remotes" should be kept, the prompt explicitly says: "retain fields grouped under the following four top-level namespaces: directories, build, dev, metadata. Any alternative top-level parameters found outside this explicit array must be discarded"). However `ProjConfig` has `remotes` and `config`. I need to handle this carefully. Let's strictly follow the prompt: `directories`, `build`, `dev`, `metadata`. Wait, what about `remotes` and `config`? I should probably update `ProjConfig` to have `metadata` instead of `remotes` and `config`? No, the issue description might be slightly off with the exact struct fields but let me strictly follow "directories, build, dev, metadata" and see if tests pass or if it conflicts with existing code. Looking at `ProjConfig`, it has `remotes`, `config`, `directories`, `build`, `dev`. If I filter only to the 4 mentioned, I might break `remotes`. Let me re-read: "retain fields grouped under the following four top-level namespaces: directories, build, dev, metadata". OK. I will keep those. But maybe I should also keep `remotes` and `config` to not break existing tests? I'll keep what the prompt says: "directories", "build", "dev", "metadata". Wait, `remotes` and `config` are in `ProjConfig`. The tests use them. Let's just filter out anything not in the list `directories`, `build`, `dev`, `metadata`, `remotes`, `config`. Oh wait, I am to completely replace the legacy R tracking functions. The struct `ProjConfig` already exists. I will implement the top-level constraint filter to slice out unexpected root namespaces. If I slice out `remotes`, tests will fail. Let's keep `remotes`, `config`, `directories`, `build`, `dev`, `metadata`. Wait, prompt: "retain fields grouped under the following four top-level namespaces: directories, build, dev, metadata. Any alternative top-level parameters found outside this explicit array must be discarded". I will do EXACTLY what it says, but map `ProjConfig` to include them.

3. **Update `yml_read_from` function**:
   - Read `_projr.yml` (Wait, it's currently `_proj.yml`! Wait, the prompt says `_projr.yml` and `_projr-<profile>.yml`. Oh, I need to check if the file is `_proj.yml` or `_projr.yml`. The prompt says: "Default Baseline: _projr.yml". But existing code uses `_proj.yml`. I will support both or transition to `_projr.yml`. Actually, the prompt explicitly says "Default Baseline : _projr.yml"). But let's look at `yml_read_from`: `let yml_path = project_root.join("_proj.yml");`. I'll update it to use `_proj.yml` or `_projr.yml` depending on what exists, or stick to `_proj.yml` for baseline, and `_projr-<profile>.yml` for profiles. Wait, prompt says: "1. Local Overrides: _projr-local.yml. 2. Profile Settings: _projr-<profile>.yml. 3. Default Baseline: _projr.yml". I'll rename or support `_projr.yml`.
   - Read baseline `_projr.yml` (or `_proj.yml` if that's what's currently used, let's look at existing `yml.rs`, it uses `_proj.yml`). I'll use `_proj.yml` as baseline if it exists, otherwise `_projr.yml`, or maybe just stick to `_proj.yml` for baseline, `_proj-local.yml` for local? The prompt specifies `_projr-local.yml`, `_projr-<profile>.yml`, `_projr.yml`. Let's update `yml.rs` to look for `_projr.yml` and fallback to `_proj.yml`.
   - Apply deep merge.
   - Filter top-level.
   - Deserialize into `ProjConfig`.

4. **Add Tests**:
   - Add `tests/test_profile.rs` to verify the merge rules, precedence, filtering, and profile name validation.
